<h1>Runes</h1>
<table>
  <% @runes.each do |rune| %>
  <tr>
    <td><%= link_to rune.name, rune_url(rune) %></td>
    <td><%= link_to "Edit", edit_rune_url(rune) %></td>
    <!--<td><%= link_to "Delete", rune_url(rune), :method => :delete %></td>-->
  </tr>
  <% end %>
</table>
