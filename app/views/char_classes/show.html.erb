<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
<% end %>
<h1> <%= @char.name %></h1>
<h2> <%= @char.description %></h2>
<hr>
<%= link_to "All Classes", char_classes_path %>
